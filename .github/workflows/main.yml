name: Cypress Integration With CI
on:
  push:
    branches: master

jobs:
  Test-on-chrome:
     runs-on: windows
     steps:
      - name: Checkout
        uses: actions/checkout@v4.1.1
      - name: Install Depenpedcies
        
      
      
